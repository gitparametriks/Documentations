function findFolder(pageMap, [path, ...paths]) {
  for (const item of pageMap) {
    if ("children" in item && path === item.name) {
      return paths.length ? findFolder(item.children, paths) : item;
    }
  }
}
export {
  findFolder
};
