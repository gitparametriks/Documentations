import * as react_jsx_runtime_js from 'react/jsx-runtime.js';
import { ReactElement } from 'react';
import { MDXComponents } from '../mdx.js';
import '@mdx-js/react';

declare function Playground({ source, scope, components, fallback }: {
    /**
     * String with source MDX
     *
     * @example '# hello world <br /> nice to see you'
     */
    source: string;
    /**
     * An object mapping names to React components.
     * The key used will be the name accessible to MDX.
     *
     * @example `{ ComponentName: Component }` will be accessible in the MDX as `<ComponentName/>`.
     */
    components?: MDXComponents;
    /**
     * Pass-through variables for use in the MDX content
     */
    scope?: Record<string, unknown>;
    /**
     * Fallback component for loading
     */
    fallback?: ReactElement | null;
}): react_jsx_runtime_js.JSX.Element | null;

export { Playground };
