import { TabPanelProps } from '@headlessui/react';
import { ReactNode, ReactElement } from 'react';

type TabItem = string | ReactElement;
type TabObjectItem = {
    label: TabItem;
    disabled: boolean;
};
declare function Tabs({ items, selectedIndex: _selectedIndex, defaultIndex, onChange, children, storageKey }: {
    items: (TabItem | TabObjectItem)[];
    selectedIndex?: number;
    defaultIndex?: number;
    onChange?: (index: number) => void;
    children: ReactNode;
    storageKey?: string;
}): ReactElement;
declare function Tab({ children, unmount, ...props }: TabPanelProps): ReactElement;

export { Tab, Tabs };
