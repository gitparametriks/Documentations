import { createContext, useContext } from "react";
const SSGContext = createContext({});
const useData = (key = "ssg") => useContext(SSGContext)[key];
const DataProvider = SSGContext.Provider;
SSGContext.displayName = "SSG";
export {
  DataProvider,
  useData
};
